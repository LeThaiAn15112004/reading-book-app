import { SettingsBackLink, SettingsHeader } from './components'

/** SCR-06 — App Settings (placeholder; not Reading Settings SCR-05). */
export function SettingsScreen() {
  return (
    <main>
      <SettingsHeader />
      <SettingsBackLink />
    </main>
  )
}
